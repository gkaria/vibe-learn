# AGENTS.md

Compatibility file for tooling that looks for `AGENTS.md`.

Canonical developer instructions live in [`CLAUDE.md`](./CLAUDE.md).

This repository supports Claude Code, GitHub Copilot CLI, Codex CLI, OpenCode, Grok Build, and Cursor. See `CLAUDE.md` for the multi-assistant architecture and adapter layout.
