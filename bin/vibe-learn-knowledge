#!/bin/bash
# vibe-learn-knowledge — knowledge ledger helper exposed on the Bash tool's PATH
# when installed as a Claude Code plugin. Delegates to scripts/knowledge.sh.
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/scripts/knowledge.sh" "$@"
